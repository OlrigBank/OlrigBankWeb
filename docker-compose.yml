# docker-compose.yml
version: '3.9'

services:
  web:
    build:
      context: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - FLASK_APP=server.py
    command: flask run --host=0.0.0.0 --port=8080
    restart: unless-stopped
